[package]
name = "job-vault-companion"
version = "0.1.0"
edition = "2021"
description = "Local companion for Job Vault Chrome extension"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
argon2 = "0.5"
ring = "0.17"
rand = "0.8"

[dev-dependencies]
